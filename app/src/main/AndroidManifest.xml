<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.luma">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_luma_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_luma_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LUMA">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAzXbscsxRoJ68DxIpMuCa2ezx4ZqmCdp0" />

        <activity android:name=".views.MapsActivity"/>
        <activity android:name=".ui.login.ForgetPassword" />
        <activity android:name=".ui.DrawerActivity" />
        <activity android:name=".ui.login.TermsConditions" />
        <activity android:name=".ui.signup.SignupActivity" />
        <activity android:name=".ui.login.ActivityLogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>